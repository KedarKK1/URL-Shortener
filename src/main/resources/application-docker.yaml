spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://postgres:5432/urlshortner}
    username: ${DB_USERNAME:urlshortner}
    password: ${DB_PASSWORD:urlshortner}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
