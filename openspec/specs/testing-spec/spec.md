# Testing spec

## Purpose
Define purpose

## Requirements

### Requirements: APIs MUST include MockMvc tests covering happy path and validation errors.
MockMvc tests included.

#### Scenario: APIs includes MockMvc tests covering happy path and validation errors.
- **WHEN** when ui is build with thymeleaf
- **THEN** the UI must check for rendering